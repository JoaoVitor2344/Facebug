// session_start();
// include("conecta.php");
// $id = $_SESSION['id'];

// $loggedtime = time() - 300; // 5 minutos

// $query = mysqli_query($conn, "SELECT * FROM pessoas WHERE id = '$id'");
// $dados = mysqli_fetch_assoc($query);

// if($dados['loggedin'] > $loggedtime) { echo $dados['nome'] . ' online'; }
// else { echo $dados['nome'] . ' offline'; }

// mysqli_close($conn);